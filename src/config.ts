export const config = {
  pokeApi: {
    // api endpoint that provides the pokemon names as a list
    url: `https://pokeapi.co/api/v2/pokemon/?limit=10`,
    // numbers of pokemons to play with. Must match 'limit' on url
    size: 100
  }
};
